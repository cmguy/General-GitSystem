This is the public key for the git userid installed for the backup and restore
script that runs from the fail-over server. The private keypair file is
install on both the primary and failover servers providing by-directional
from either system to either system as the git userid.

