This is the public key for the git userid installed for the backup and restore
script that runs from the fail-over server. The private keypair file is
installed on both the primary and failover servers providing by-directional
from either system to either system as the git userid. The private key
being used is only available from the primary and fail-over servers 
git/.ssh directories.

